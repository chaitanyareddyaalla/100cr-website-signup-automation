# Copy to .env for local Docker use. Never commit the real file or credentials.
APP_ENV=development
AUTOMATION_MODE=mock
GOOGLE_SHEETS_ID=
GOOGLE_SHEETS_WORKSHEET=Results
# This path must exist inside the backend container if Google export is enabled.
GOOGLE_SERVICE_ACCOUNT_JSON=
